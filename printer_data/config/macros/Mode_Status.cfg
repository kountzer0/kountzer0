[gcode_macro MODE_STATUS]
description: Display current printer mode
gcode:
  {% if printer.save_variables.variables.active|default(0)|int == 1 %}
    RESPOND TYPE=command MSG="MODE: FACTORY (mechanical endstops)"
  {% else %}
    RESPOND TYPE=command MSG="MODE: PERFORMANCE (sensorless homing)"
  {% endif %}