[gcode_macro SAFE_HOME_Z]
gcode:
  {action_respond_info("Homing Z (top endstop)...")}

  # Home Z first
  G28 Z

  # Now Z is homed at the top
  # Move down to bed (logical 0)
  G1 Z0 F3000

  {action_respond_info("Z homed at top, moved to 0 (bed surface).")}
