[gcode_macro Z_SANITY_CHECK]
description: "Refuse XY moves unless Z is homed"
gcode:
  {% if "z" not in printer.toolhead.homed_axes %}
    RESPOND TYPE=error MSG="Z is not homed. Refusing XY movement."
    M400
    CANCEL_PRINT
  {% endif %}
